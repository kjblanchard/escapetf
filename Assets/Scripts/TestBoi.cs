using UnityEngine;

public class TestBoi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Going to the store prolly");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
